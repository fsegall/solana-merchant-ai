# 🚀 Deploy to Vercel

## Build Success! ✅

Build completed successfully! The project is ready for deployment to Vercel.

## Deployment Steps

### 1. Connect Repository to Vercel

1. Visit [vercel.com](https://vercel.com)
2. Sign in with your GitHub account
3. Click **"Add New Project"**
4. Select repository: `fsegall/solana-merchant-pay`

### 2. Configure Environment Variables

In Vercel dashboard, add the following environment variables:

#### **Supabase**
```
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key
```

#### **Solana**
```
VITE_SOLANA_NETWORK=devnet
VITE_SOLANA_ENDPOINT=https://api.devnet.solana.com
VITE_MERCHANT_WALLET=YOUR_WALLET_PUBKEY
```

#### **Helius (Optional, but recommended)**
```
VITE_HELIUS_API_KEY=your-helius-api-key
```

#### **Payments**
```
VITE_PAYMENT_PROVIDER=solana-pay
VITE_JUPITER_API_URL=https://quote-api.jup.ag/v6
```

#### **Settlement (Optional for demo)**
```
VITE_ENABLE_SETTLEMENTS=true
```

### 3. Automatic Configuration

The `vercel.json` file is already configured with:
- ✅ Build command: `npm run build`
- ✅ Output directory: `dist`
- ✅ Framework: Vite
- ✅ Rewrites for SPA routing
- ✅ Security headers
- ✅ CORS configured for `manifest.json`

### 4. Deploy

Vercel will deploy automatically when you:
1. Connect the repository
2. Configure environment variables
3. Click **"Deploy"**

### 5. Next Steps

After deployment:

1. **Test the Deployment:**
   - Visit the URL generated by Vercel
   - Test login/signup
   - Test order creation
   - Test QR code generation

2. **Configure Edge Functions (If applicable):**
   - Supabase Edge Functions continue to work
   - URLs: `https://your-project.supabase.co/functions/v1/function-name`

3. **Update README.md:**
   - Add deployment link
   - Document the process

## 🔗 Important Links

- **Repository:** https://github.com/fsegall/solana-merchant-pay
- **Supabase:** [your-project].supabase.co
- **Vercel Dashboard:** https://vercel.com/dashboard

## ⚠️ Original Issue Resolved

The `exports is not defined` error was resolved with:
- Configuration of `output.format: 'es'` in Vite
- Configuration of `esmExternals: true` in CommonJS
- Build completed successfully without critical errors

## 📝 Notes

- **Large chunks:** Some chunks are >500kB, but acceptable for MVP
- **Demo Mode:** Settlement is in DEMO mode for hackathon
- **Devnet:** Currently configured for Solana Devnet (testnet)